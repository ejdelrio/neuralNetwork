'use strict';

const expect = require('chai').expect;


describe('Initial test for sigmoid functions', () => {
  it('Should run a basic test for now', () => {
    expect(2 + 2).to.equal(4);
  });
});
